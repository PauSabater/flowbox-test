---
import Layout from '../layouts/Layout.astro'
import { App } from '@components/App/App'

const response = await fetch('https://flowbox-test-pau-sabater.vercel.app/api/images?category=nature')
const data = await response.json()

---

<Layout title='Technical test for Flowbox'>
	<App client:only apiInitialResponse={data.results}/>
</Layout>

